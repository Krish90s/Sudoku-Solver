import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Python-Solver',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
